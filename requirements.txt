# Core dependencies
numpy>=1.20.0
psutil>=5.8.0
# kicad-python 0.6+ moved to pynng 0.9 (Python 3.12-3.14 wheels);
# 0.7.x adds the KiCad 10 endpoints. Works against KiCad 9 and 10.
kicad-python>=0.7.1,<0.8

# GUI dependencies (optional)
PyQt6>=6.0.0

# GPU acceleration (optional — install the one matching your hardware):
#   NVIDIA/CUDA:         pip install cupy-cuda12x  (or cupy for source build)
#   Apple Silicon/Metal: pip install mlx           (requires Python >= 3.10)
# cupy>=10.0.0

# Development dependencies (optional)
pytest>=6.0.0
pytest-cov>=2.0.0
black>=21.0.0
flake8>=3.8.0
mypy>=0.800

# Documentation dependencies (optional)  
sphinx>=4.0.0
sphinx-rtd-theme>=0.5.0